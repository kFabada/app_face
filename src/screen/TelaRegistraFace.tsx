import React from 'react';
import { Button, View } from 'react-native';
import CameraComponet from '../components/CameraComponent';

export function TelaRegistraFace({ navigation }) {
  return (
    <CameraComponet/>
  );
}